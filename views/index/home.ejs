<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Home</title>
  </head>
  <body>
    <h1>HOME</h1>

    <% if(!currentUser){ %>
        <a class="" href="/auth/google">Sign in with Google</a>
    <% } else { %>
        <p>Signed in as <%= currentUser.firstName + " " + currentUser.lastName %></p>
        <a href="/auth/logout">Sign out</a>
    <% } %>

  </body>
</html>
